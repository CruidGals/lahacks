/**
 * The frontend used to seed itself from this file. Real data is now served
 * by the backend (lib/api.ts → lib/http.ts). The only thing that lives here
 * is the default map center used before geolocation resolves.
 */
export const DEFAULT_LOCATION = { lat: 34.0689, lng: -118.4452 };
